<?php
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

if ( ! function_exists( 'wp_password_change_notification' ) ) :

    function wp_password_change_notification( $user ) {
    // We are not sending the notification
    }

endif;
