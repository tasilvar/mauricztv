<?php
add_shortcode('mjDisplayCrosseling', 'mjDisplayCrosseling');

function mjDisplayCrosseling() { 
/*
    $args = array(
		'post_type'      => 'download',
		'post_status' => 'publish',
		'posts_per_page' => -1,
		'meta_key' => 'sales_disabled',
		'meta_value' => 'off',
        'meta_query' => $filterArgs
	);

    $getProducts = get_posts($args);

    // Zwroc liste produktów w koszyku
    foreach($getProducts as $procuct) { 
        // Jesli produkt zawiera ID inne niz te ktore jest w koszyku dodaj do listy
    }
    */

    return "##### ####";
}